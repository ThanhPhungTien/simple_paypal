name: simple_paypal
description: Simple Paypal plugin
version: 1.0.0
homepage: https://github.com/ThanhPhungTien
publish_to: https://infra-repository.citigo.net/api/packages/kms/pub

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.24.3"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  plugin:
    platforms:
      android:
        package: vn.thanhpt.simple_paypal
        pluginClass: SimplePaypalPlugin
